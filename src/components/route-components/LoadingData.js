import React from "react";
const Loader = () => {
  return (
    <section className="section-loader">
      <h3 className="section-loader__title">loading...</h3>
    </section>
  );
};

export default Loader;
